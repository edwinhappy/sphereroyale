# Sphere Royale — Environment Variables
# Copy this file to .env and server/.env, then fill in your values.

# ============================================================================
# FRONTEND (root .env) — exposed to browser via import.meta.env
# Only VITE_* vars are safe to put here. Never put secrets in this file.
# ============================================================================

# Backend API Endpoint
# VITE_API_URL=http://localhost:3001/api

# Backend WebSocket Endpoint
# VITE_WS_URL=http://localhost:3001

# TON recipient wallet address (for receiving Jetton USDT payments)
VITE_TON_RECIPIENT_ADDRESS=UQABMDiCrsUcY-CvMvo4uYhOPWextvu23L0FmWhJMNCCIaFR

# Solana recipient wallet address (for receiving SPL USDT payments)
VITE_SOL_RECIPIENT_ADDRESS=...your-solana-address...

# Solana RPC endpoints (comma-separated for fallback)
VITE_SOL_RPC_ENDPOINTS=https://api.mainnet-beta.solana.com,https://solana-api.projectserum.com

# TON USDT Jetton master contract address (mainnet)
VITE_TON_USDT_JETTON_MASTER=EQCxE6mUtQJKFnGfaROTKOt1lZbDiiX1kCixRv7Nw2Id_sDs

# Solana USDT SPL token mint address (mainnet)
VITE_SOL_USDT_MINT=Es9vMFrzaCERmJfrF4H2FYD4KCoNkY11McCe8BenwNYB

# ============================================================================
# BACKEND (server/.env) — never exposed to the browser
# ============================================================================

# MongoDB connection string (required)
# MONGODB_URI=mongodb://localhost:27017/sphereroyale
REDIS_URI=redis://localhost:6379

# JWT secret for admin token signing — generate a random 64-char hex string (required)
# JWT_SECRET=...your-random-secret...

# JWT token expiry duration (default: 15m)
# JWT_EXPIRY=15m

# Admin password hash — SHA-256 of your chosen password (required)
# ADMIN_PASSWORD_HASH=...sha256-hash...

# Payment verification — addresses and mints (all required)
# SOL_RECIPIENT_ADDRESS=...your-solana-address...
# TON_RECIPIENT_ADDRESS=...your-ton-address...
# SOL_USDT_MINT=Es9vMFrzaCERmJfrF4H2FYD4KCoNkY11McCe8BenwNYB
# TON_USDT_JETTON_MASTER=EQCxE6mUtQJKFnGfaROTKOt1lZbDiiX1kCixRv7Nw2Id_sDs

# Solana RPC endpoints (comma-separated for fallback, default: https://api.mainnet-beta.solana.com)
# SOL_RPC_ENDPOINTS=https://api.mainnet-beta.solana.com,https://solana-api.projectserum.com

# TON RPC endpoints (comma-separated TonCenter v3 API compliant endpoints)
# TON_RPC_ENDPOINTS=https://toncenter.com/api/v3

# Registration fee in USDT (default: 2)
# REGISTRATION_FEE_USDT=2

# Server port (default: 3001)
# PORT=3001
